cron:
  - description: "arb chance report"
    url: /cron/start/bot
    schedule: "every 3 minutes"
    timezone: UTC
    retry_parameters:
      max_backoff_seconds: 90
      max_doublings: 1
      job_retry_limit: 2
